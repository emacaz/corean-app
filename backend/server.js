/**
 * Main backend entry point.
 *
 * Responsibilities:
 * - Start the Express server.
 * - Register global middlewares (CORS, JSON parsing, logging).
 * - Mount feature routes (e.g., /tts).
 * - Keep a single entry for configuring and running the backend.
 */
